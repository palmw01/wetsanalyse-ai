[project]
name = "wetsanalyse-bronmodel"
version = "0.1.0"
description = "Canonieke bronboom en lokale tekstankers voor Wetsanalyse"
requires-python = ">=3.11"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["bronmodel"]
